steps:
  - name: "Test unsafe script 01"
    plugin:
      name: unsafe
      required:
        commands:
          - hostname
          - whoami
        files:
          - /etc/passwd
          - missing_file
      bash: |
        echo 'AAA' > file.txt
        hostname >> file.txt
        whoami >> file.txt
        echo "BBB" >> file.txt
