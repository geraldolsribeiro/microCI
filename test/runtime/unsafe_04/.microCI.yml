steps:
  - name: "Test unsafe script 01"
    plugin:
      name: unsafe
      required:
        commands:
          - hostname
          - missing_command
          - whoami
        files:
          - /etc/passwd
      bash: |
        echo 'AAA' > file.txt
        hostname >> file.txt
        whoami >> file.txt
        echo "BBB" >> file.txt
