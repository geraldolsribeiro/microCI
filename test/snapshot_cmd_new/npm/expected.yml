#   # npm plugin
#   
#{{{
docker: "node:16"
steps:
  - name: "Install dependencies"
    description: "Step description"
    bash: |
      npm install
  - name: "Check code"
    bash: |
      npm run lint --fix
  - name: "Build"
    bash: |
      npm run build
#}}}

# vim: spell spelllang=en
